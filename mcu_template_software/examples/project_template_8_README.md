# Example Project 8

This example has all the functionality of example 6 plus the ability to set the time using the ntp protocol.
The MCU must have internet connectivity to allow ntp to synchronize the time.


Note that this file must have a line feed character at the end of the line.
